# Project-specific ProGuard rules.
# For best results, enable shrinking + obfuscation in release builds and verify
# with `assembleRelease`. Add keep rules here when reflection or kept classes
# are needed (Room entities, Hilt modules, etc. are handled by their plugins).
