<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Launch theme used briefly before Compose takes over. enableEdgeToEdge()
         + MCNMTTheme handle real colors at runtime, so this stays minimal and
         avoids dragging in the com.google.android.material XML library. -->
    <style name="Theme.MCNMT" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
